/**
 * @param {string} s
 * @param {string} p
 * @return {boolean}
 */
var isMatch = function(s, p) {
    let dp = new Array()
    for (let i = 0; i <= s.length; i++) {
        dp[i] = new Array()
    }
    dp[0][0] = true
    for (let i = 0; i <= s.length; i++) {
        for (let j = 0; j <= p.length; j++) {
            
        }        
    }
};